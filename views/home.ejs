<html>

<head>
    <title>Home page</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
</head>

<body>
    <div class="container">
        <h2>Todo Manager</h2>
        <a href="/create" class="btn btn-success float-right">Create todo</a>
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>Todo id</th>
                    <th>Task</th>
                    
                    <th>Edit</th>
                </tr>
            </thead>
            <% todo.forEach((entry) => { %>
            <tr ondblclick="handleUpdateProduct('<%= entry.id %>')">
                <td><%= entry.id %></td>
                <td><%= entry.task %></td>
                
                <td>
                    <input class="btn btn-primary" type="button" value="Update"
                        onclick="handleUpdateProduct('<%= entry.id %>')">
                </td>
            </tr>
            <% }) %>
        </table>
        <script>
            function handleUpdateProduct(value) {
                $(location).attr('href', '/update/' + value);
            }
        </script>
    </div>
</body>

</html>